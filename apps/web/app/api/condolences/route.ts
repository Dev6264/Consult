import { NextResponse } from 'next/server'; import { prisma } from '@/lib/prisma'; import { throttle } from '@/lib/rateLimit';
export async function POST(req:Request){ const form=await req.formData(); const ip=req.headers.get('x-forwarded-for')||'local'; if(throttle(`cond-${ip}`,10,60000)) return NextResponse.json({error:'Rate limited'},{status:429}); await prisma.condolence.create({data:{obituaryId:String(form.get('obituaryId')),authorName:String(form.get('authorName')),message:String(form.get('message'))}}); return NextResponse.redirect(new URL(req.headers.get('referer')||'/',req.url)); }
