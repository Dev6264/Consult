import Link from 'next/link'; import { prisma } from '@/lib/prisma'; import { isAdmin } from '@/lib/auth'; import { redirect } from 'next/navigation';
export default async function AdminHome(){ if(!(await isAdmin())) redirect('/admin/login'); const queue=await prisma.obituary.findMany({where:{status:{in:['SUBMITTED','SUBMITTED_HIGH_RISK','NEEDS_INFO','VERIFIED']}},orderBy:{createdAt:'desc'}}); return <div className='space-y-2'><h1 className='text-xl font-bold'>Moderation Queue</h1><div className='flex gap-3 underline'><Link href='/admin/watchlist'>Watchlist</Link><Link href='/admin/condolences'>Condolences</Link><Link href='/admin/leads'>Leads</Link><Link href='/admin/alerts'>Alerts</Link></div>{queue.map(q=><Link key={q.id} href={`/admin/obituaries/${q.id}`} className='block border bg-white p-2'>{q.fullName} - {q.status}</Link>)}</div>; }
