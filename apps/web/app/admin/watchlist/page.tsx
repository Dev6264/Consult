import { prisma } from '@/lib/prisma'; import { isAdmin } from '@/lib/auth'; import { redirect } from 'next/navigation';
export default async function Watchlist(){ if(!(await isAdmin())) redirect('/admin/login'); const names=await prisma.watchlistName.findMany({orderBy:{createdAt:'desc'}}); return <div><form action='/api/admin/watchlist' method='post' className='flex gap-2'><input className='border p-2' name='name'/><input className='border p-2' name='reason'/><button className='bg-black text-white px-3'>Add</button></form>{names.map(n=><p key={n.id}>{n.name} - {n.reason}</p>)}</div>; }
