import { prisma } from '@/lib/prisma'; import { isAdmin } from '@/lib/auth'; import { redirect, notFound } from 'next/navigation';
export default async function Review({params}:{params:Promise<{id:string}>}){ if(!(await isAdmin())) redirect('/admin/login'); const {id}=await params; const ob=await prisma.obituary.findUnique({where:{id},include:{documents:true,auditLogs:true}}); if(!ob) notFound(); return <div className='space-y-2'><h1 className='text-xl font-bold'>{ob.fullName}</h1><p>{ob.status} / {ob.riskFlag}</p><form action='/api/admin/obituaries/update' method='post' className='grid gap-2 max-w-lg'><input type='hidden' name='id' value={ob.id}/><select name='status' className='border p-2'><option>NEEDS_INFO</option><option>VERIFIED</option><option>PUBLISHED</option><option>REJECTED</option></select><textarea name='internalNotes' className='border p-2'/><label><input type='checkbox' name='isPremium'/>Premium</label><button className='bg-black text-white p-2'>Save</button></form></div>; }
